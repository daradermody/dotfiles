#!/usr/bin/bash

CURRENT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
source ${CURRENT_DIR}/aliases
source ${CURRENT_DIR}/environment_variables
source ${CURRENT_DIR}/functions
source ${CURRENT_DIR}/shell_settings

